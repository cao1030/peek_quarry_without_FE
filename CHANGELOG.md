# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的结构，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.0.0] - 2026-09-22

第一个稳定版。

### 新增

- **「末影之触」方块**（`peek_quarry:ender_touch`）。右键打开扫描界面。
  - 贴图目前是**占位图**：由原版命令方块贴图染成绿色，见 `tools/recolor_command_block.py`。
  - **合成配方**（工作台）：
    ```
    铁块      末影之眼   铁块
    末影之眼   钻石镐    末影之眼
    铁块      末影之眼   铁块
    ```
    mod 启动时会做一次配方自检并把结果打进日志，改坏配方会直接报 `配方自检失败`。
- **区块扫描**：以方块所在区块为中心，半径 0–8（1×1 到 17×17 = 289 区块）。
  服务端强制加载范围内区块并逐区块统计所有方块，按 `Block + metadata` 计数。
  - 剔除**流体**（材质为液体，或实现 Forge 的 `IFluidBlock`）与**基岩**。
  - 分摊到多个 tick，带进度条，可随时取消并保留部分结果。
- **统计界面**：可滚动列表（图标 + 本地化名称 + 数量），按数量升降序切换。
- **搜索**：中文子串、英文名，以及**拼音**（全拼 / 首字母缩写 / 多音字任一读音 / `lv` 与 `lu`）。
  拼音表由 Unihan 数据生成，20901 字，按需懒加载。
- **提取入容器**：中键点统计列表中的方块 → 弹窗选数量（输入框，`+1` / `+100` / `+1000` / 全部，
  右键输入框清空）→ 确认后服务端在已扫描范围内**真实挖掉**该数量的方块，
  以「种类 + 数量」的形式存进方块的**虚拟存储**（不是实体物品栏：一次扫描可能挖出几十万个方块）。
  - 提取同样分摊到多个 tick，可取消；已挖的部分照常进容器。
  - 挖掘顺序按到中心区块的切比雪夫距离，从方块身边向外扩散。
- **容器侧边栏**：界面右侧只读列表，显示容器里已存放的物品，可滚动。
- **主动输出**：方块空闲时每 8 tick 尝试一次，把容器里的物品推进**相邻容器**
  （六个面各最多 64 个）。遵守 `ISidedInventory` 的槽位访问与 `canInsertItem`，
  以及 `isItemValidForSlot`。没有对应 `ItemStack` 的方块（甘蔗、农作物、水/岩浆）无法输出。
- **NBT 持久化**：扫描结果、容器内容、半径与状态都写进 TileEntity。

### 说明

- 扫描结果、进度与容器内容通过自定义网络包 + Container 窗口属性同步到客户端。
- 仅支持 **Minecraft 1.7.10 + Forge 10.13.4.1614**。

### 已知问题

- **未验证**：主动输出把物品**真正塞进相邻容器**的那一步，没有在实机上跑通过。
  输出循环本身已被日志证实每 8 tick 在执行并查找相邻 `IInventory`，
  但因为开发机的键盘输入失效，无法可靠地摆放容器做端到端验证。
  详见 `docs/DEV_NOTES.md` 的「未验证项」。
- 「提取」只在单方块、单人、最多约 1.8 万个方块的场景下验证过。
- 未生成区块的现场地形生成路径没有被真正触发过（测试世界视距大于半径上限）。
- `en_US` 语言文件没有在游戏内切换英文验证过。
- 不支持中英/中拼混输的搜索（例如 `shi头`）。

### 未包含

- **不含 FE（Forge Energy）相关的任何内容** —— 本仓库即 `without_FE` 版本，
  modid 仍是 `peek_quarry`。

[1.0.0]: https://github.com/cao1030/peek_quarry_without_FE/releases/tag/v1.0.0
