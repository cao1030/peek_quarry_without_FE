# 统一换行符，避免在不同平台上 checkout 后出问题。
#
# 为什么需要这个文件：Windows 的 cmd.exe 解析批处理**必须**用 CRLF，
# 只有 LF 的话会把行从中间拆开执行（本项目的 dev.bat 就真的这样炸过一次，
# 报一堆 `'tlocal' is not recognized` 之类的碎片错误）。
# 靠 core.autocrlf 是不够的 —— 它取决于每个人的本地配置。

* text=auto eol=lf

# Windows 批处理必须是 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# Gradle wrapper 的 shell 脚本必须是 LF
gradlew text eol=lf

# 二进制，别做任何转换
*.jar binary
*.png binary
*.zip binary
*.ogg binary
